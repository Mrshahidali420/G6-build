{
  "name": "gta6record",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/build-data.mjs && astro build && npx -y pagefind --site dist && node scripts/after-build.mjs",
    "data": "node scripts/build-data.mjs",
    "preview": "astro preview",
    "check": "astro check"
  },
  "dependencies": {
    "astro": "^5.14.1",
    "@astrojs/sitemap": "^3.6.0"
  },
  "devDependencies": {
    "pagefind": "^1.4.0"
  },
  "allowScripts": {
    "esbuild@0.27.7": true,
    "esbuild@0.25.12": true,
    "sharp@0.34.5": true
  }
}
