{
  "name": "gta6record",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/build-data.mjs && astro build && node scripts/after-build.mjs",
    "data": "node scripts/build-data.mjs",
    "preview": "astro preview",
    "check": "astro check"
  },
  "dependencies": {
    "astro": "^5.14.1",
    "@astrojs/sitemap": "^3.6.0"
  },
  "devDependencies": {
    "pagefind": "^1.4.0"
  }
}
